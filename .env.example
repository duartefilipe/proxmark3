# Copie para .env (ou preencha no Portainer em "Environment variables")

# Banco de dados (Postgres ja existente: container postgres-anakin)
PM3_PGHOST=192.168.31.229
PM3_PGPORT=15432
PM3_PGDATABASE=proxmark
PM3_PGUSER=proxmark
PM3_PGPASSWORD=troque_essa_senha_forte

# Painel web
PM3_WEB_PORT=8787

# Device do Proxmark3 no host (confira com: ls -l /dev/ttyACM*)
PM3_DEVICE=/dev/ttyACM0
